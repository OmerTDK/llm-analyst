__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.env
.DS_Store
uv.lock
target/
platform/target/
dbt_packages/
platform/dbt_packages/
logs/
*.duckdb
# The pre-built semantic fixture is intentionally committed — it is the stable
# input to CI (no dbt build in CI). Exception to the *.duckdb rule above.
!tests/fixtures/semantic_fixture.duckdb
data/local/
